I can do two: easy and difficult

or maybe it can increase if player go far away in the levels
and use an if for changing css depending of the numbers of cells in a row

Using levels id ? Lets say 10 levels with cells of 40px and then it is the grid with cells of 20px ?
